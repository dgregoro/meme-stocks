# Plan: S6 slow pairs (deferred)

1. Choose v1 input: **adjusted** daily closes from existing `price_data` vs dedicated CA pipeline.
2. Define spread / z-score / half-life estimation (pure functions first).
3. Evaluation: forward returns from spread signals (not per-leg toy unless documented).
4. Integration: optional link to leader-follower or standalone CLI.

**Current state:** Spec placeholder only.
