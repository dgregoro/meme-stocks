# Quickstart: 021 Strategy S3 (placeholder)

**Status:** Not implemented — this file will hold example commands after delivery.

**Planned examples** (from [spec.md](./spec.md)):

```bash
# Ingest macro vol (exact CLI TBD during implementation)
# python -m backend.app.cli backfill vol-term  # or research macro ...

# Single-symbol eval (parallel to s1/s2)
# python -m backend.app.cli evaluate daily-strategy s3 --symbol SPY --start YYYY-MM-DD --end YYYY-MM-DD

# Merit + persistence (parallel to s1-merit)
# python -m backend.app.cli evaluate daily-strategy s3-merit --symbols-file ... --start ... --end ...
```

See [docs/STRATEGY_TESTING_PLAN.md](../../docs/STRATEGY_TESTING_PLAN.md) § S3 for methodology.
