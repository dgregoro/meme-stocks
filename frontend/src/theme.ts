/**
 * Typography and spacing scale (PRD §5.3).
 * Use these for consistent hierarchy and scannable layout.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const

export const fontFamily = 'system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif'

export const fontSize = {
  sm: 12,
  base: 14,
  md: 16,
  lg: 18,
  xl: 20,
} as const
