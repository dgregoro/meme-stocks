[pytest]
minversion = 7.0
addopts = -ra
testpaths = 
    backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast, isolated tests
    integration: tests involving DB or external services (mocked where possible)
